# Database (use docker-compose.dev.yml for local dev — host port 5433)
DATABASE_URL="postgresql://grosir:grosir_dev@localhost:5433/grosir_dev?schema=public"

# NextAuth (generate your own secret: openssl rand -base64 32)
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# App
APP_TIMEZONE="Asia/Jakarta"
LOG_LEVEL="info"
